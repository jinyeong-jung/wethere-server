type UpdateMyProfileResponse {
  ok: Boolean!
  error: String
}

enum statusOptions {
  HAPPY
  DEPRESSED
  MAD
  ENERGIZED
  UNCERTAIN
  PEACEFUL
  CONFUSED
}

type Mutation {
  UpdateMyProfile(
    nickname: String
    gender: String
    birth: String
    status: statusOptions
    profilePhoto: String
    phoneNumber: String
  ): UpdateMyProfileResponse!
}
